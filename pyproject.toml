[project]
name = "sig-pages"
version = "0.1.0"
description = "GitHub Pages automation scripts"
requires-python = ">=3.11"
dependencies = [
    "markdown>=3.7",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = [".github/scripts"]
